rightWristY=0;
leftWristY=0;
scoreRightWrist=0;
scoreLefttWrist=0;
song="";
function setup(){
    canvas=createCanvas(400,400);
    canvas.position(500,200);
    video=createCapture(VIDEO);
    video.hide();
}
